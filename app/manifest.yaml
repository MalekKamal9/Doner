---
applications:
  - name: donor-management-bita
    memory: 256M
    disk_quota: 512M
    buildpacks:
      - nodejs_buildpack
    command: npx cds-serve
    random-route: true
    env:
      NODE_ENV: production
    services:
      - donor-management-hana
      - donor-management-xsuaa
